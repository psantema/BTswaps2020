.onLoad <- function(libname, pkgname){
  packageStartupMessage("Data and scripts
  to open the code run:
  file.edit(paste0(.libPaths(), '/BTswaps2020/data.r'))
  to open the code for randomisations run:
  file.edit(paste0(.libPaths(), '/BTswaps2020/random.r'))")
 }
